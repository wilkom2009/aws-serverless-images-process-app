{
  "name": "aws-serverless-images-process-app",
  "version": "0.1.0",
  "bin": {
    "aws-serverless-images-process-app": "bin/aws-serverless-images-process-app.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.20.0",
    "jest": "^26.4.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.20.0",
    "constructs": "^10.0.0"
  }
}
